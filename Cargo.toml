[package]
name = "empty_directml"
version = "0.1.0"
edition = "2021"

[lib]
name = "DirectML"
crate-type = ["cdylib"]

[profile.release]
opt-level = 3
lto = true
codegen-units = 1
debug = false

[build-dependencies]

[dependencies]
